# 🚀 Как запустить бота 24/7 бесплатно (Render.com)

Этот гайд поможет вам загрузить бота на бесплатный хостинг, чтобы он работал всегда, даже когда ваш компьютер выключен.

## Шаг 1: Подготовка (GitHub)
Вам нужно загрузить код бота на GitHub.
1.  Зарегистрируйтесь на [GitHub.com](https://github.com/).
2.  Создайте **New Repository** (назовите его, например, `discord-voice-bot`).
3.  Сделайте его **Private** (чтобы никто не украл код), если хотите, но для Render проще **Public** (но тогда **НЕ ЗАГРУЖАЙТЕ** файл `.env`!).
    > **ВАЖНО**: Файл `.env` с токеном **НЕЛЬЗЯ** загружать в интернет. Мы добавим токен в настройках хостинга.
4.  Загрузите файлы: `main.py`, `requirements.txt`, `Dockerfile`, `allowed_users.json` (если есть).

## Шаг 2: Регистрация на Render
1.  Перейдите на [Render.com](https://render.com/).
2.  Нажмите **Get Started for Free**.
3.  Войдите через **GitHub**.

## Шаг 3: Создание сервиса
1.  В панели Render нажмите **New +** -> **Web Service**.
2.  Выберите ваш репозиторий `discord-voice-bot`.
3.  **Name**: Любое имя.
4.  **Region**: Frankfurt (ближе к нам).
5.  **Branch**: `main`.
6.  **Runtime**: **Docker**.
7.  **Instance Type**: **Free**.

## Шаг 4: Настройка переменных (Environment Variables)
Это самый важный шаг! Здесь мы введем токен.
Найдите раздел **Environment Variables** и добавьте:

| Key | Value |
| :--- | :--- |
| `DISCORD_TOKEN` | Ваш токен бота (тот длинный) |
| `ADMIN_ID` | Ваш ID (цифры) |

## Шаг 5: Запуск
Нажмите **Create Web Service**.
Render начнет сборку (Build). Это займет пару минут.
Если увидите в логах `Logged in as ...`, значит бот работает!
> **Примечание**: Если вы ранее видели ошибку "Timed out" или "Port scan timeout", последнее обновление исправляет это, запуская небольшой веб-сервер вместе с ботом.


## ⚠️ Важное про бесплатный тариф Render
На бесплатном тарифе бот будет "засыпать", если его не трогать 15 минут.
Когда вы напишете команду, он проснется, но это займет ~30-60 секунд.

**Альтернатива (Railway.app)**:
Дает $5 кредитов в месяц (хватает на 20 дней работы 24/7).

**Альтернатива (Ваш ПК)**:
Просто не выключайте компьютер. 😄
